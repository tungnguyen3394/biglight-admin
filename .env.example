# Sao chép thành .env trên VPS rồi điền giá trị thật: cp .env.example .env

# --- Database (PostgreSQL dùng chung ở ~/infra, database RIÊNG: biglight_web) ---
DATABASE_URL=postgres://crm_user:MATKHAU@postgres:5432/biglight_web

# --- Google login (Google Cloud Console > OAuth Client ID, loại Web) ---
GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com

# --- Email được phép đăng nhập admin (cách nhau dấu phẩy) ---
ADMIN_EMAILS=bboyvnm331994@gmail.com

# --- Session (chuỗi ngẫu nhiên >= 32 ký tự: openssl rand -hex 32) ---
SESSION_SECRET=THAY_BANG_CHUOI_NGAU_NHIEN

NODE_ENV=production

# --- Email tự động (Gmail/Workspace SMTP) ---
SMTP_USER=n-tung@biglight.jp
SMTP_PASS=APP_PASSWORD_16_KY_TU
MAIL_FROM=BIGLIGHT株式会社 <n-tung@biglight.jp>
ADMIN_NOTIFY_TO=n-tung@biglight.jp
